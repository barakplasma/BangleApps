// Stub for @hebcal/core DailyLearning.js — only used when options.dailyLearning is set,
// which we never set.
export var DailyLearning = {
  has: function() { return false; },
  lookup: function() { return null; },
};
